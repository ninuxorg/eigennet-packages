This package will provide lite eigennet firmware.
This is fully compatible with the mesh but the client access only with ipv4 to limit size of package.
In this way we can remove dibbler-server and radvd from dependency.
