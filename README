This git repo contains the ninux trunk OpenWRT packages with custom fixes
and improvement for a simple autoconfiguration of your device.


To use this repo in your OpenWRT buildroot simple add this line to the feeds:

src-git eigennet git://gitorious.org/eigennet/packages.git;roma 

or

src-git eigennet git://github.com/arkanet/eigennet-packages.git


Then use the follow commands to update/install packages from the new repo:
openwrt$ ./scripts/feeds update -a
openwrt$ ./scripts/feeds install -a
openwrt$ make menuconfig
Enter in section Administration and in eigennet select all packages.
